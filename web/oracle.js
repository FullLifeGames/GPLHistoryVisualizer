// Six-degrees graph over played matches. Implemented in Phase 3 Task 6;
// stubs keep the module graph loadable until then.

export function oracleGraph() {
  return { nodes: new Map(), edges: new Map() };
}

export function oraclePath() {
  return null;
}

export function connectednessRows() {
  return [];
}
